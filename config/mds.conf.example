# MDS config example

MDS_DB_PATH=/tmp/zb_mds_rocks
SCHEDULER_ADDR=127.0.0.1:9100
SCHEDULER_REFRESH_MS=2000
CHUNK_SIZE=4194304
REPLICA=2
# If each node group already has primary-secondary replication, set REPLICA=1 to avoid over-replication.
ENABLE_OPTICAL_ARCHIVE=false
ARCHIVE_TRIGGER_BYTES=10737418240
ARCHIVE_TARGET_BYTES=8589934592
COLD_FILE_TTL_SEC=3600
ARCHIVE_SCAN_INTERVAL_MS=5000
ARCHIVE_MAX_CHUNKS_PER_ROUND=64

# Node list format:
#   node_id@address[,type=REAL|VIRTUAL][,weight=N][,virtual_node_count=N]
# Positional fields are also accepted:
#   node_id@address,VIRTUAL,10,100000
NODES=node-01@127.0.0.1:19080,type=REAL,weight=1;node-02@127.0.0.1:19081,type=REAL,weight=1;node-03@127.0.0.1:19082,type=REAL,weight=1

# Disk list per node: node_id:disk1,disk2,disk3
DISKS=node-01:disk-01,disk-02,disk-03;node-02:disk-01,disk-02,disk-03;node-03:disk-01,disk-02,disk-03

# Example virtual pool (single endpoint simulates many nodes):
# NODES=vpool@127.0.0.1:29080,type=VIRTUAL,weight=10,virtual_node_count=100000;node-01@127.0.0.1:19080,type=REAL,weight=1
# DISKS=vpool:disk-01,disk-02,disk-03;node-01:disk-01,disk-02,disk-03

# Example with optical node:
# ENABLE_OPTICAL_ARCHIVE=true
# NODES=node-01@127.0.0.1:19080,type=REAL,weight=1;vpool@127.0.0.1:29080,type=VIRTUAL,weight=8,virtual_node_count=100000;optical-01@127.0.0.1:39080,type=OPTICAL,weight=1
# DISKS=node-01:disk-01,disk-02,disk-03;vpool:disk-01,disk-02,disk-03;optical-01:disk-01,disk-02,disk-03

# If SCHEDULER_ADDR is set and Scheduler is healthy, Scheduler view will override these static lists.
