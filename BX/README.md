# BX Workspace

`BX/code` contains a standalone Linux C++17 implementation of:

- CPU multithread CDC dedup program
- Versions V0/V1/V2/V3
- Sharded dedup index and pipeline runtime
- Dataset generator (A/B/C), validation, CSV/JSON output, bench script

Build/run instructions: see `BX/code/README.md`.
